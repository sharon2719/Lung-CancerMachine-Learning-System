<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.LungradarApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:forceDarkAllowed">false</item> <!-- Ensures light mode -->
        <item name="android:windowFullscreen">true</item> <!-- Hides the status bar -->
        <item name="android:windowNoTitle">true</item>
    </style>
</resources>